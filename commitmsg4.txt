fix(lint): restore ESLint with flat config

Phase 15 (design 55):
- eslint.config.js (flat config) + typescript-eslint@8 + @eslint/js@9
  compatible with eslint 9; legacy .eslintrc.json retired
- Fixed 23 accumulated no-unused-vars / no-useless-escape errors across
  enricher, resolver, transport, relay/*, registry/*, enhance-config,
  profiles, loader (dead code from refactors; lint never ran before)
- lint script updated for flat config; npm run lint now passes cleanly
- Typecheck + 368 tests still pass
