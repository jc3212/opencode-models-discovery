fix(release): SemVer valid version + node-runnable CLI

Phase 1-2 (design 5-11):
- npm already has stable 1.4.0, so 1.4.0-rc.1 was SemVer-invalid
  (1.4.0-rc.1 < 1.4.0). Bumped to 1.5.0-rc.1.
- CLI is now a compiled node bundle (Option B, design 10):
  bun build src/cli.ts --target=node -> dist/cli.js with
  #!/usr/bin/env node; prepack builds it; files: ["src","dist"];
  bin -> ./dist/cli.js
- scripts/build-cli.sh + scripts/test-cli-bunless.sh
- bun-less test: PATH without bun, tarball installed, npx-style bin
  runs audit, 0 credential leak -> PASS
- cli.ts resolves the bundled registry robustly in both dist/ and
  src/ layouts (design 12-13)
- test-package now requires package/dist/cli.js in the tarball
